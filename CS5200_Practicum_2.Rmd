---
title: "Practicum_Two"
output:
  pdf_document: default
  html_notebook: default
---

```{r}
library(RSQLite)

fpath = "/Users/legen/Desktop/PracticumTwo"
dbfile = "PracticumTwo.sqlite"

dbcon <- dbConnect(RSQLite::SQLite(), paste0(fpath,dbfile))
```

```{sql connection=dbcon}
DROP TABLE IF EXISTS Author;
```

```{sql connection=dbcon}
DROP TABLE IF EXISTS Jounal;
```

```{sql connection=dbcon}
CREATE TABLE Journal (
  ISSN TEXT PRIMARY KEY,
  isoabbreviation TEXT NOT NULL,
  title TEXT NOT NULL
);
```

```{sql connection=dbcon}
CREATE TABLE Author (
  authorId INTEGER PRIMARY KEY,
  lastName TEXT,
  foreName TEXT,
  initials TEXT
);
```

Loading Libraries

```{r warning=FALSE}
library(XML)
library(DBI)
library(knitr)
library(sqldf)
options(sqldf.driver = "SQLite")
options(warn=-1)
```

```{r}
path <- "/Users/legen/Desktop/PracticumTwo/"
fn <- "pubmed_sample.xml"
file = paste0(path, fn)
xmlDOM <- xmlParse(file)
r <- xmlRoot(xmlDOM)
size <- xmlSize(r)
```

## 1.Inspect the pubmed Catalog XML 

```{r warning = FALSE}
xparse <- xmlParse("pubmed_sample.xml")
xparse
```

## 2.Load the pubmed Catalog XML directly into a dataframe using xmlToDataFrame.

```{r}
xmldata <- xmlToDataFrame("pubmed_sample.xml")
print(xmldata)
```




